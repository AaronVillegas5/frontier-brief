name = "frontier-brief-oauth"
main = "index.js"
compatibility_date = "2024-01-01"

# Secrets are set via: wrangler secret put GITHUB_CLIENT_ID
# and: wrangler secret put GITHUB_CLIENT_SECRET
# They are encrypted at rest and never appear in this file.
